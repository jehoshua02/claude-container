# Add epic handling to project management framework

Epics are big rocks. Split into smaller rocks until they fit in inbox, then refine as usual. Need to define the process and document in project/README.md.

## Smaller rocks

- Add epics/ folder and epic workflow to README
- Rescale prioritization from 1-3 to 1-5 (done in README, need to rescore existing tasks) — cherry-picked into prioritization-updates task
- Rescore existing 1-todo tasks on new 1-5 scale — cherry-picked into prioritization-updates task
- Dependency-aware scoring: blocked tasks score low, rescore when dependency completes
