# Move project management instructions to a Claude plugin

Extract the project management workflow from `project/README.md` into a reusable Claude plugin. This would allow the same task management system to be used across multiple projects without duplicating the instructions.
