# Add sensible defaults to settings.default.json

Configure permissions, allowed tools, and model selection in `settings.default.json` so the container ships with safe, opinionated defaults.

## Priority: 05

| Factor | Score | Reasoning |
|---|---|---|
| Value | 2 | Better out-of-box experience, safer defaults |
| Momentum | 1 | Not started |
| Effort | 2 | Need to research what Claude Code settings exist and pick good defaults |
| Risk | 2 | Wrong defaults could restrict users unexpectedly. Need to understand settings schema. |
