# Add sensible defaults to settings.default.json

Configure permissions, allowed tools, and model selection in `settings.default.json` so the container ships with safe, opinionated defaults.

## Priority: 059

| Factor | Score | Reasoning |
|---|---|---|
| Value | 5/10 — Better out-of-box experience, safer defaults |
| Momentum | 2/10 — Not started; no related completed tasks |
| Effort | 5/10 — Need to research what Claude Code settings exist and pick good defaults |
| Risk | 5/10 — Wrong defaults could restrict users unexpectedly. Need to understand settings schema. |
