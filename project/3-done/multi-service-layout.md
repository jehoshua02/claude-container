# Move service files into services/claude/ subfolder

Moved Dockerfile, entrypoint.sh, plugins.sh, settings.default.json into `services/claude/`. Updated docker-compose.yml build context. Root-level files stay at root. Ready for additional services.

## Completed
